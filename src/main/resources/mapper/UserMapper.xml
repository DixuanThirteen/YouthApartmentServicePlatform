<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.yasp.mapper.UserMapper">
    <select id="selectByUsername" resultType="com.yasp.entity.User">
        SELECT * FROM users WHERE username = #{username}
    </select>

    <insert id="insertUser" parameterType="com.yasp.entity.User">
        INSERT INTO users(username, password, phone, email, age, born_date, sex)
        VALUES (#{username}, #{password}, #{phone}, #{email}, #{age}, #{bornDate}, #{sex})
    </insert>

    <!-- 更新除密码外的基本信息 -->
    <update id="updateUser" parameterType="com.yasp.entity.User">
        UPDATE users
        SET nickname = #{nickname},
            phone = #{phone},
            email = #{email},
        WHERE username = #{username}
    </update>

    <!-- 单独更新密码 -->
    <update id="updatePassword" parameterType="com.yasp.entity.User">
        UPDATE users
        SET password = #{password}
        WHERE username = #{username}
    </update>

    <delete id="deleteByUsername" parameterType="string">
        DELETE FROM users WHERE username = #{username}
    </delete>

    <update id="updateAvatar" parameterType="map">
        UPDATE users
        SET avatar = #{avatar}
        WHERE username = #{username}
    </update>

    <update id="updateNickname" parameterType="map">
        UPDATE users
        SET nickname = #{nickname}
        WHERE username = #{username}
    </update>

    <update id="updateIntroduction" parameterType="map">
        UPDATE users
        SET introduction = #{introduction}
        WHERE username = #{username}
    </update>
</mapper>